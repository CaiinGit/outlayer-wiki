services:
  outlayer-wiki:
    image: nginx:alpine
    container_name: outlayer-wiki
    restart: unless-stopped
    ports:
      - "192.168.1.197:8081:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
